node_modules
dist
coverage
test-results
playwright-report
demo/dist
demo/.next
demo/node_modules
demo/dev.log
*.tsbuildinfo
.DS_Store


# npm credentials (never commit)
.npmrc
# IDE / editor
.vscode/
.idea/
.kilo/

# Local editor worktrees

# Dependencies are committed for application lockfile reproducibility.
# These exceptions override common user-level package-lock ignore rules.
!package-lock.json
!demo/package-lock.json

